{
  "name": "email_reader",
  "version": "0.1.0",
  "description": "Extract news links from emails and summarize via a Cloudflare Worker backend and Tampermonkey client.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1",
    "wrangler": "^4.47.0"
  }
}
